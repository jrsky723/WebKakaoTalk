extends ../base

block content
    div.center
        form(method="POST" action="/chats/create" )
            input(name="name" placeholder="Title", required, type="text", )
            input(type="submit", value="Create Room")