extends ../base

block content
    h2= `Delete ${loggedInUser.name}'s Account?`
    form(method="POST")
        label Password:
            input(type="password", name="password", required)
        div.center
            input(type="submit", value="Delete")
            a(href=`/users/${loggedInUser}profile`) Back
        
