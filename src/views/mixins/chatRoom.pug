mixin chatRoom(room)
    div
        h4
            a(href=`/chats/${room.id}`)= room.title
        ul
            li #{room.title}
            li #{room.userCount}
            li #{room.lastMessage}
            li #{room.updatedAt}
            