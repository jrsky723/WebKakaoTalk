mixin chatRoom(room)
    div
        h4
            a(href=`/chats/${room.id}`)= room.title
        ul
            li #{room.name}
            li #{room.max}
            li #{room.createdAt}
            li #{room.updatedAt}
            